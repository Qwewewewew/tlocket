version: '3'
services:

  pgdb:
    environment:
    - PGDATA=/var/lib/postgresql/data/locketdb
    - POSTGRES_PASSWORD=asdf1234
    image: 'postgres:latest'
    ports:
    - 5432:5432
    restart: always
    volumes:
    - ./pgdb:/docker-entrypoint-initdb.d:ro
